# Omni-G Documentation

This documentation set is now split into two tracks:

- `V1/` preserves the pre-overhaul architecture, roadmap, and tech-stack baseline.
- `V2/` defines the overhaul target aligned to the intelligence-cycle workflow in `overhaul.md`.

Use `V1/` when you need historical context for the current implementation. Use `V2/` when planning or implementing the overhaul.

## Index

- `V1/README.md` — archived documentation index
- `V2/README.md` — overhaul documentation index
- `agent-contexts/gap-matrix.md` — live delta tracker during migration
